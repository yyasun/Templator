using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace Templator
{
    public class TemplateProcessor
    {
        // should it have side effects? or not? Yes, probably/
        // also I want it to be a tree
        public void Process()
        {
            // process all tags
            // register the selector
            // register variables
        }
    }
}